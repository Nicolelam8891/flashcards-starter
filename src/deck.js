const createDeck = (deckArray) => {
  return deckArray;
}

const countCards = (deckArray) =>  {
  let numberOfCards = deckArray.length
  return numberOfCards;
}

module.exports = {
  createDeck,
  countCards,
};